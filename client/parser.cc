#include "parser.hh"
#include <iostream>

// parser::streamParser
// Description - stream을 
// Return - True(성공), False(실패)
bool parser::streamParser(BYTE* stream){
}

// gpg::recvPubKey
// Description - PubKeyID에 해당하는 PublicKey를 keyserver로부터 받는 gpg --recv-key를 수행(fork-exec) 사용
// Return - True(성공), False(실패)
bool parser::message㎩rser(BYTE* stream , message _message){
}

// gpg::recvPubKey
// Description - PubKeyID에 해당하는 PublicKey를 keyserver로부터 받는 gpg --recv-key를 수행(fork-exec) 사용
// Return - True(성공), False(실패)
bool list㎩rser(BYTE* stream, nodelist curNodeList){
}

// gpg::recvPubKey
// Description - PubKeyID에 해당하는 PublicKey를 keyserver로부터 받는 gpg --recv-key를 수행(fork-exec) 사용
// Return - True(성공), False(실패)
bool hbParser(BYTE* stream, heartbeat recvBeat){
}

// gpg::recvPubKey
// Description - PubKeyID에 해당하는 PublicKey를 keyserver로부터 받는 gpg --recv-key를 수행(fork-exec) 사용
// Return - True(성공), False(실패)
BYTE* packMessage(String recvGithubID, nodelist curNodeList, String Contents){
}

BYTE* packNode(String GithubID, String IP, String PubKeyID){
}

// gpg::recvPubKey
// Description - PubKeyID에 해당하는 PublicKey를 keyserver로부터 받는 gpg --recv-key를 수행(fork-exec) 사용
// Return - True(성공), False(실패)
BYTE* packHeartBeat(BYTE* OTK, long Timestamp){
}

